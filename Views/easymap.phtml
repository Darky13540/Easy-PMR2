    <main class="maineasymap">

        <section class="filtres">
            <form action="easymap" method="post">
            <label for="name"><i class="fas fa-search-location"></i></label>
            <input type="text" name="name" >
            <input type="text" name="genre" placeholder="Magasin, lieu public...">
            <input type="text" name="type" placeholder="Boulangerie, supermarché...">


                <!-- idée à retenir -->
            <!-- <select name="genre">
            <?php foreach ($genres as $genre):?>
            <option value="<?=ucfirst(htmlspecialchars($genre['genre']))?>"><?=ucfirst(htmlspecialchars($genre['genre']))?></option>

            <?php endforeach;?>
                    </select>

                    <select name="type">
            <?php foreach ($types as $type):?>
            <option value="<?=ucfirst(htmlspecialchars($type['type']))?>"><?=ucfirst(htmlspecialchars($type['type']))?></option>

            <?php endforeach;?>
                    </select> -->


            <button type="submit" class="btnForm">OK/RAZ</button>
            </form>
            <?php 
            if ($count>0){
                echo "<p>Il y a " .$count. " résultat(s) à votre recherche</p>";
            }else{
                echo "<p>Il n'y a aucun résultat à votre recherche!</p>";
            }
            ?>
        </section>

        <div class="mapListContainer">
            <div id="mapid"></div>

            <div class="listpoi">

                <?php foreach ($poi as $item): ?>
                    <div class="item js-marker"                    
                    data-id="<?= htmlspecialchars($item['id']) ?>"
                    data-type="<?= htmlspecialchars($item['type']) ?>"
                    data-name="<?= htmlspecialchars($item['name']) ?>"
                    data-lat="<?= floatval($item['lat']) ?>"
                    data-long="<?= floatval($item['longitude']) ?>">
                    <a href="details?id=<?=$item['id'] ?>" title="page de détails">
                <img src="https://fakeimg.pl/200/" alt="photo poi">
                <h2><?= htmlspecialchars( $item['name']) ?></h2>
                <p><?= htmlspecialchars($item['type']) ?></p>
                <p><?= htmlspecialchars($item['genre']) ?></p>
                </a>
            </div>
                <?php endforeach;?>
            </div>
            <div id="scroll_to_top">
                <a href="easymap#top"><img src="assets/img/icons/top.png" alt="Retourner en haut" /></a>
            </div>  
    </main>

    <!-- Chargement librairie JS Leaflet pour affichage de la carte -->
    <script src="https://unpkg.com/leaflet@1.7.1/dist/leaflet.js"
   integrity="sha512-XQoYMqMTK8LvdxXYG3nZ448hOEQiglfqkJs1NOQV44cWnUrBc8PkAOcXy20w0vlaXaVUearIOBhiXZ5V3ynxwA=="
   crossorigin=""></script>

   <!-- Chargement JS MAP -->
    <script src="assets/js/easymap.js"></script>
